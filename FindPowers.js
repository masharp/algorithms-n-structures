/*  This is an implementation of an algorithm to recursively find all of the powers of a number.

    by Michael Sharp
    www.softwareontheshore.com
    msharp.oh@gmail.com
    ---------------------------------
    Psudocode:

*/
var num = Math.floor(Math.random() * 10);
var result = findPowers(num);

console.log("Random: " + num);
console.log(result);

function findPalindrome(num) {
}
