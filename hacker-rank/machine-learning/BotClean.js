/**
 * JavaScript Solution to HackerRank BotClean AI Challenge
 * https://www.hackerrank.com/challenges/botclean
 *
 * The bot is on a grid of either clean or dirty positions. Command the bot
 * to clean the floor in the shortest amount of steps
 */

const testA = '0 0\nb---d\n-d--d\n--dd-\n--d--\n----d'

function cleanFloor(input) {
  const robot = input.split('\n')[0].split(' ');
  const map = input.split('\n').slice(1, 6);

  
}

cleanFloor(testA);
